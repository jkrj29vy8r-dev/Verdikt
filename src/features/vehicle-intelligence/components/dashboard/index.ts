/**
 * Dashboard command center — the authenticated home's widget suite. Import
 * `CommandCenter` (a Server Component that composes the client widget islands)
 * by this path from the dashboard page; the individual widgets and the
 * `DashboardModel` builders (`../../dashboard`) stay feature-internal.
 */
export * from "./command-center";
